# Clients

Create one client folder for every customer.

Example:

```text
clients/
├── abc-salon/
├── rahul-ca/
└── neuroedge-solutions/
```

Copy `../client-template` for each new client.

For normal updates, edit only the client's `data.js` and replace their images.
